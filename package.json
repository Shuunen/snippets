{
  "$schema": "https://json.schemastore.org/package",
  "author": "Romain Racamier-Lafon <romain.racamier@gmail.com>",
  "bugs": "https://github.com/Shuunen/snippets/issues",
  "dependencies": {
    "7zip-min": "^3.0.1",
    "cli-progress": "^3.12.0",
    "clipboardy": "^5.3.1",
    "cors": "^2.8.6",
    "es-toolkit": "^1.49.0",
    "exiftool-vendored": "^37.0.0",
    "sharp": "^0.35.3"
  },
  "description": "My personal snippets",
  "devDependencies": {
    "@types/cli-progress": "^3.11.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "@vitest/ui": "^4.1.10",
    "bun": "^1.3.14",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "oxlint-tsgolint": "^0.25.0",
    "shuutils": "~10.6.0",
    "tiny-glob": "^0.2.9",
    "turbo": "^2.10.5",
    "typescript": "~7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  },
  "homepage": "https://github.com/Shuunen/snippets",
  "keywords": [
    "personal",
    "snippets"
  ],
  "license": "GPL-3.0",
  "name": "@shuunen/snippets",
  "packageManager": "pnpm@10.30.3",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shuunen/snippets.git"
  },
  "scripts": {
    "build": "turbo run build:tsc && echo build completed successfully || (echo Please fix build errors and run pnpm run build to check for remaining build issues && exit 1)",
    "build:tsc": "tsc -b && echo build:tsc completed successfully || (echo Please fix type errors and run pnpm run build:tsc to check for remaining type issues && exit 1)",
    "check": "turbo run lint:types lint:format lint:issues lint:snippets build:tsc test:unit && echo check completed successfully",
    "config:sync": "bun configs/bin/sync.cli.js",
    "cs": "bun config:sync",
    "lint": "turbo run lint:types lint:format lint:issues && echo lint completed successfully",
    "lint:format": "oxfmt --check && echo lint:format completed successfully || (echo Please run pnpm run lint:format:fix to automatically fix formatting issues && exit 1)",
    "lint:format:fix": "oxfmt --write && echo lint:format:fix completed successfully || (echo Please run pnpm run lint:format to check for remaining formatting issues && exit 1)",
    "lint:issues": "oxlint --type-aware --deny-warnings --report-unused-disable-directives && echo lint:issues completed successfully || (echo Please run pnpm run lint:issues:fix to automatically fix linting issues && exit 1)",
    "lint:issues:fix": "oxlint --fix --fix-suggestions --fix-dangerously --type-aware --deny-warnings --report-unused-disable-directives && echo lint:issues:fix completed successfully || (echo Please run pnpm run lint:issues to check for remaining linting issues && exit 1)",
    "lint:snippets": "bun src/bin/lint.cli.ts --target=manuals/**/*.md && echo lint:snippets completed successfully || (echo Please fix snippet issues && exit 1)",
    "lint:types": "tsc -p tsconfig.app.json && tsc -p tsconfig.node.json && echo lint:types completed successfully || (echo Please fix type errors and run pnpm run lint:types to check for remaining type issues && exit 1)",
    "test": "turbo run test:unit",
    "test:ui": "vitest --ui",
    "test:unit": "vitest run --coverage && echo test:unit completed successfully || (echo Please fix test errors and run pnpm run test:unit to check for remaining test issues && exit 1)",
    "test:watch": "vitest",
    "up:deps": "pnpm update --latest && echo up:deps completed successfully"
  },
  "type": "module",
  "version": "0.1.0"
}
