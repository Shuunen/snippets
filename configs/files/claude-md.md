@RTK.md

# graphify

- **graphify** (`~/.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
  When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# Asking questions

Always ask me **one question at a time**, never a batch. Each question must have :

1. an ELI10 paragraph explaining it in detail (what it means, why it matters, what changes per answer),
2. several numbered pre-made answers, with your recommendation marked,
3. always an explicit free-text option so I can answer something else.
   Wait for my answer before asking the next one.
